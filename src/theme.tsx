import { createMuiTheme } from '@material-ui/core/styles';


const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2a2a2a',
    },
    secondary: {
      main: '#444',
    },
    background: {
      default: '#fff',
    },
  },
});

export default theme;
